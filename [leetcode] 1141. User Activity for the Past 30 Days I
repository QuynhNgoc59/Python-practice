LINK: https://leetcode.com/problems/user-activity-for-the-past-30-days-i/description/?envType=problem-list-v2&envId=m8baczxh
TYPE: Easy

==> My approach:
import pandas as pd

def user_activity(activity: pd.DataFrame) -> pd.DataFrame:
    # Get the time frame of 30 days before 2019-07-27
    end_date = pd.to_datetime('2019-07-27')
    start_date = end_date - pd.Timedelta(days=29)
    
    # Calculate the number of users per day within this time frame 
    num_users = (
        activity[activity['activity_date'].between(start_date, end_date)]
        .groupby(by='activity_date')['user_id']
        .nunique()
        .reset_index()
        .rename(columns={'activity_date': 'day', 'user_id': 'active_users'})
    )
    return num_users
