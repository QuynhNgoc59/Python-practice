LINK: https://leetcode.com/problems/article-views-i/description/?envType=problem-list-v2&envId=v9r4nua2
TYPE: Easy

==> My approach:
import pandas as pd

def article_views(views: pd.DataFrame) -> pd.DataFrame:
    # Find authors viewing her/his own articles
    authors = (
        views
        .query('viewer_id == author_id')
        .groupby(by='author_id')
        .nunique()
        .reset_index()
        .rename(columns={'author_id': 'id'})[['id']]
    )
    return authors
