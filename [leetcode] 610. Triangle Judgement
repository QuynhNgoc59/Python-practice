LINK: https://leetcode.com/problems/triangle-judgement/description/?envType=problem-list-v2&envId=m8baczxh
TYPE: Easy

==> My approach:
import pandas as pd

def triangle_judgement(triangle: pd.DataFrame) -> pd.DataFrame:
    def triangle_identify(row):
        if  (
            row['x'] + row['y'] > row['z'] 
            and row['x'] + row['z'] > row['y']
            and row['y'] + row['z'] > row['x']
        ):
            return 'Yes'
        else:
            return 'No'
    triangle['triangle'] = triangle.apply(triangle_identify, axis=1)

    return triangle
